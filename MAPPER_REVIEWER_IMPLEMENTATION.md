# VeriSafe 매퍼-검수자 시스템 구현 완료

## 📋 개요

위성지도 기반 매핑 및 검수 시스템이 **플랫폼 분리 전략**으로 구현되었습니다.

### 핵심 설계 원칙

**문제점:** 일반 유저에게 복잡한 매핑/검수 UI를 노출하면 UX가 혼란스러워짐

**해결책:** 플랫폼 분리
- **모바일 앱** (React Native): 일반 유저 전용 - 검증된 데이터만 표시
- **웹 포털** (Next.js): 매퍼/검수자 전용 - 정밀 작업에 최적화

## 🎯 구현 범위

### ✅ 완료된 항목

#### 1. 백엔드 API (FastAPI) - 100% 완료
- ✅ 매퍼 API (`/api/mapper/*`)
  - 지리 정보 생성/수정/삭제
  - 내 기여 목록 및 통계
  - 중복 검사 (50m 반경)
- ✅ 검수자 API (`/api/review/*`)
  - 검수 대기열 조회
  - 승인/거부 워크플로우
  - 지도 영역별 조회
  - 대시보드 통계
- ✅ 데이터베이스 모델 (`detected_features`)
  - 검수 상태 관리
  - 리뷰어 코멘트
  - 타임스탬프

#### 2. 웹 포털 (Next.js) - 100% 완료
- ✅ 프로젝트 초기화
  - Next.js 16 + TypeScript
  - Tailwind CSS
  - Leaflet + Leaflet.draw
- ✅ 인증 시스템
  - JWT 토큰 기반
  - Zustand 상태 관리
  - 역할 기반 접근 제어
- ✅ 매퍼 인터페이스
  - 위성지도 편집기
  - GeoJSON 드로잉 도구
  - 내 기여 목록 및 통계
- ✅ 검수자 인터페이스
  - 검수 대기열
  - 승인/거부 패널
  - 검수 대시보드

#### 3. 모바일 앱 연동 - 100% 완료
- ✅ 프로필 탭에 웹 포털 링크 추가
- ✅ 역할 기반 조건부 표시 (mapper, admin만)
- ✅ 플랫폼별 URL 처리

#### 4. 문서 - 100% 완료
- ✅ 웹 포털 README
- ✅ 사용자 가이드
- ✅ 실행 스크립트 (START_WEB_PORTAL.bat)

## 📂 프로젝트 구조

```
verisafe/
├── backend/                            # FastAPI 백엔드
│   └── app/
│       ├── routes/
│       │   ├── mapper.py              # 매퍼 API ✅
│       │   ├── reviewer.py            # 검수자 API ✅
│       │   └── detected_feature.py    # AI 탐지 API ✅
│       ├── services/
│       │   ├── mapper_service.py      # 매퍼 비즈니스 로직 ✅
│       │   ├── review_service.py      # 검수 비즈니스 로직 ✅
│       │   └── satellite/
│       │       └── image_analyzer.py  # 위성 이미지 분석 ✅
│       ├── models/
│       │   └── detected_feature.py    # DB 모델 ✅
│       └── schemas/
│           └── detected_feature.py    # API 스키마 ✅
│
├── mobile/                             # React Native 앱
│   └── src/
│       └── screens/
│           └── ProfileTabScreen.js    # 웹 포털 링크 ✅
│
├── web-portal/                         # Next.js 웹 포털 ⭐ 신규
│   ├── app/
│   │   ├── (portal)/
│   │   │   ├── mapper/
│   │   │   │   ├── page.tsx          # 지도 편집기 ✅
│   │   │   │   └── contributions/    # 내 기여 ✅
│   │   │   └── reviewer/
│   │   │       ├── page.tsx          # 검수 대기열 ✅
│   │   │       └── dashboard/        # 대시보드 ✅
│   │   ├── login/                     # 로그인 ✅
│   │   └── layout.tsx                 # 루트 레이아웃 ✅
│   ├── components/
│   │   ├── layout/
│   │   │   ├── Sidebar.tsx           # 사이드바 ✅
│   │   │   └── Header.tsx            # 헤더 ✅
│   │   └── map/
│   │       └── MapEditor.tsx         # Leaflet 지도 ✅
│   ├── lib/
│   │   ├── api/
│   │   │   ├── client.ts             # Axios 클라이언트 ✅
│   │   │   ├── auth.ts               # 인증 API ✅
│   │   │   ├── mapper.ts             # 매퍼 API ✅
│   │   │   └── reviewer.ts           # 검수자 API ✅
│   │   └── stores/
│   │       └── auth-store.ts         # 인증 상태 ✅
│   └── types/
│       └── index.ts                   # TypeScript 타입 ✅
│
├── START_WEB_PORTAL.bat               # 웹 포털 실행 ✅
└── WEB_PORTAL_GUIDE.md                # 사용자 가이드 ✅
```

## 🔄 워크플로우

### 매퍼 워크플로우

```
1. 웹 포털 로그인 (mapper/admin 계정)
   ↓
2. 사이드바 > 지도 편집
   ↓
3. 위성 이미지에서 건물/도로 확인
   ↓
4. Leaflet.draw로 객체 그리기
   - 마커 (Point) - 병원, 학교 등
   - 선 (LineString) - 도로
   - 다각형 (Polygon) - 건물
   ↓
5. 정보 입력 폼 작성
   - 객체 유형 선택
   - 이름, 설명 입력
   ↓
6. 제출 → POST /api/mapper/features
   ↓
7. review_status: 'pending'으로 저장
   ↓
8. 내 기여 페이지에서 상태 추적
```

### 검수자 워크플로우

```
1. 웹 포털 로그인 (admin 계정)
   ↓
2. 사이드바 > 검수 대기열
   ↓
3. 필터 선택 (전체/매퍼/AI)
   ↓
4. 테이블에서 항목 선택 → "검수하기"
   ↓
5. 우측 패널에서 상세 정보 확인
   ↓
6. 판단:
   - 승인 → POST /api/review/{id}/approve
     → review_status: 'approved', verified: true
     → 공개 지도에 반영
   - 거부 → POST /api/review/{id}/reject
     → review_status: 'rejected'
     → 매퍼에게 피드백
   ↓
7. 검수 대시보드에서 통계 확인
```

## 🎨 주요 기능

### 1. 지도 편집기 (MapEditor.tsx)

**기술:**
- Leaflet.js
- Leaflet.draw
- 위성 이미지 타일 (ArcGIS)
- OpenStreetMap 레이블 (반투명)

**기능:**
- 📍 마커 그리기 → Point
- 📏 선 그리기 → LineString
- ⬛ 다각형 그리기 → Polygon
- ✏️ 편집 및 삭제
- 🔄 GeoJSON 자동 변환

### 2. 검수 대기열 (reviewer/page.tsx)

**기능:**
- 📋 FIFO 정렬 (오래된 순)
- 🔍 출처별 필터링 (AI/매퍼/전체)
- 📊 신뢰도 표시 (0~100%)
- ✅ 승인/❌ 거부 액션
- 💬 검수 코멘트

### 3. 검수 대시보드 (reviewer/dashboard/page.tsx)

**통계:**
- 검수 대기 건수
- 검수 중 건수
- 오늘 승인 건수
- 오늘 거부 건수
- 출처별 대기 현황 (AI vs 매퍼)
- 승인 비율

## 🔐 역할 기반 접근 제어

| 역할 | 모바일 앱 | 웹 포털 | 지도 편집 | 검수 |
|------|-----------|---------|-----------|------|
| user | ✅ | ❌ | ❌ | ❌ |
| mapper | ✅ | ✅ | ✅ | ❌ |
| admin | ✅ | ✅ | ✅ | ✅ |

## 🚀 실행 방법

### 1. 백엔드 시작

```bash
# 기존 방법대로
SETUP.bat
```

### 2. 웹 포털 시작

```bash
# 새로운 스크립트 사용
START_WEB_PORTAL.bat
```

또는

```bash
cd web-portal
npm install
npm run dev
```

### 3. 접속

- 백엔드: http://localhost:8000
- 웹 포털: http://localhost:3000
- 모바일 앱: http://localhost:8081

### 4. 테스트 계정 생성

백엔드에서 매퍼/관리자 계정 생성:

```python
# backend/scripts/create_test_users.py (예시)
from app.models import User
from app.services.auth_service import get_password_hash

# 매퍼 계정
mapper = User(
    username='mapper1',
    email='mapper1@verisafe.com',
    password_hash=get_password_hash('password123'),
    role='mapper'
)

# 관리자 계정
admin = User(
    username='admin',
    email='admin@verisafe.com',
    password_hash=get_password_hash('admin123'),
    role='admin'
)
```

## 📊 데이터베이스 스키마

### detected_features 테이블

```sql
CREATE TABLE detected_features (
    -- Identity
    id UUID PRIMARY KEY,
    feature_type VARCHAR(50), -- building, road, hospital, etc.

    -- Location
    latitude FLOAT,
    longitude FLOAT,
    geometry_type VARCHAR(20), -- point, line, polygon
    geometry_data JSONB, -- GeoJSON geometry

    -- Detection
    confidence FLOAT, -- 0.0 ~ 1.0
    detection_source VARCHAR(50), -- mapper_created, ai, satellite_ai

    -- Review Workflow
    review_status VARCHAR(20), -- pending, under_review, approved, rejected
    created_by_user_id UUID, -- 매퍼
    reviewed_by_user_id UUID, -- 검수자
    review_comment TEXT,
    reviewed_at TIMESTAMP,

    -- Verification
    verified BOOLEAN,
    verification_count INT,

    -- Metadata
    name VARCHAR(200),
    description TEXT,
    properties JSONB,

    -- Timestamps
    created_at TIMESTAMP,
    updated_at TIMESTAMP,
    last_verified_at TIMESTAMP
);
```

## 🌐 API 엔드포인트

### 매퍼 API

```
POST   /api/mapper/features              # 지리 정보 생성
PUT    /api/mapper/features/{id}         # 수정
DELETE /api/mapper/features/{id}         # 삭제 (pending만)
GET    /api/mapper/my-contributions      # 내 기여 목록
GET    /api/mapper/my-summary            # 통계
```

### 검수자 API

```
GET    /api/review/pending               # 검수 대기열
POST   /api/review/{id}/start-review     # 검수 시작
POST   /api/review/{id}/approve          # 승인
POST   /api/review/{id}/reject           # 거부
GET    /api/review/area                  # 지도 영역 조회
GET    /api/review/dashboard             # 대시보드 통계
```

## 💡 핵심 기술 결정

### 1. 플랫폼 분리

**선택:** 웹 포털을 별도 프로젝트로 분리

**이유:**
- ✅ 각 사용자 그룹에 최적화된 UX
- ✅ 모바일 앱 단순화 (일반 유저 혼란 방지)
- ✅ 정밀 작업은 큰 화면에서 (마우스, 키보드)
- ✅ 코드베이스 분리로 유지보수 용이

**대안:**
- ❌ 모바일 앱에 조건부 UI → 복잡도 증가, 터치 조작 어려움
- ❌ 하이브리드 → 두 플랫폼 모두 개발 필요

### 2. Leaflet vs Google Maps

**선택:** Leaflet.js

**이유:**
- ✅ 오픈소스, 무료
- ✅ Leaflet.draw 플러그인 완성도 높음
- ✅ GeoJSON 네이티브 지원
- ✅ 위성 이미지 타일 자유 선택

**대안:**
- ❌ Google Maps → 비용, API 키 관리
- ❌ Mapbox → 상업용 라이선스

### 3. Next.js vs CRA

**선택:** Next.js (App Router)

**이유:**
- ✅ SSR/SSG 지원 (SEO 고려)
- ✅ 파일 기반 라우팅
- ✅ 이미지 최적화
- ✅ 타입스크립트 내장 지원

## 🔜 향후 개선 사항

### 단기 (1-2주)

- [ ] 지도에서 feature 시각화 (검수 시)
- [ ] 이미지 업로드 기능
- [ ] 실시간 알림 (WebSocket)
- [ ] 벌크 작업 (여러 항목 동시 승인)

### 중기 (1-2개월)

- [ ] 검수 통계 차트 (recharts)
- [ ] 변경 이력 추적 (audit log)
- [ ] 고급 필터링 (날짜, 지역, 유형)
- [ ] 검수자 간 할당 시스템

### 장기 (3개월+)

- [ ] AI 보조 검수 (자동 추천)
- [ ] 매퍼 랭킹 및 보상 시스템
- [ ] 지역별 매퍼 권한 (특정 지역만 편집)
- [ ] 다국어 지원

## 📖 참고 문서

프로젝트 내 문서:
- `web-portal/README.md` - 기술 문서
- `WEB_PORTAL_GUIDE.md` - 사용자 가이드
- `backend/app/routes/mapper.py` - 매퍼 API 코드
- `backend/app/routes/reviewer.py` - 검수자 API 코드

외부 자료:
- [Leaflet 공식 문서](https://leafletjs.com/)
- [Leaflet.draw](https://github.com/Leaflet/Leaflet.draw)
- [Next.js App Router](https://nextjs.org/docs/app)
- [React Query](https://tanstack.com/query/latest)

## ✅ 검증 체크리스트

### 백엔드

- [x] 매퍼 API 모든 엔드포인트 작동
- [x] 검수자 API 모든 엔드포인트 작동
- [x] 중복 검사 로직 (50m)
- [x] 역할 기반 권한 검증
- [x] CORS 설정 (웹 포털 URL 포함)

### 웹 포털

- [x] 로그인 및 인증
- [x] 지도 편집기 드로잉
- [x] GeoJSON 변환 및 제출
- [x] 내 기여 목록 조회
- [x] 검수 대기열 조회
- [x] 승인/거부 액션
- [x] 대시보드 통계

### 모바일 앱

- [x] 웹 포털 링크 표시 (mapper/admin만)
- [x] URL 오픈 동작

### 문서

- [x] README 작성
- [x] 사용자 가이드 작성
- [x] 실행 스크립트 작성
- [x] 구현 요약 문서 (본 문서)

## 🎉 결론

VeriSafe의 매퍼-검수자 시스템이 **플랫폼 분리 전략**을 통해 성공적으로 구현되었습니다.

**핵심 성과:**
- ✅ **백엔드 100% 완성**: 모든 API 프로덕션 준비 완료
- ✅ **웹 포털 100% 완성**: Next.js 기반 매퍼/검수자 UI
- ✅ **모바일 연동 완료**: 역할 기반 웹 포털 접근
- ✅ **문서화 완료**: README, 가이드, 스크립트

**효과:**
- 🎯 **UX 분리**: 일반 유저는 단순한 앱, 전문가는 강력한 웹 도구
- 🚀 **생산성 향상**: 큰 화면과 정밀한 도구로 매핑 효율 극대화
- ✅ **품질 보증**: 체계적인 검수 워크플로우로 데이터 신뢰도 확보
- 📊 **투명성**: 실시간 통계로 작업 현황 파악

이제 OpenStreetMap, Mapillary와 같은 크라우드소싱 지도 서비스와 동등한 수준의 시스템을 갖추었습니다!

---

**VeriSafe 팀**
