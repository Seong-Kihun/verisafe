# VeriSafe 앱 UX 개선 계획서

> 작성일: 2025-11-05
> 목적: 사용자 친화적인 지도 경로 탐색 앱으로 개선

---

## 📋 목차

1. [현재 문제점 분석](#1-현재-문제점-분석)
2. [경쟁 앱 UX 패턴 분석](#2-경쟁-앱-ux-패턴-분석)
3. [개선 방안](#3-개선-방안)
4. [구현 계획](#4-구현-계획)
5. [예상 효과](#5-예상-효과)

---

## 1. 현재 문제점 분석

### 1.1 사용자 흐름이 복잡함

**문제:**
- 검색 → 장소 선택 → 경로 찾기의 흐름이 직관적이지 않음
- 여러 시트(PlaceDetailSheet, RouteResultSheet, RouteHazardBriefing)가 겹쳐서 혼란스러움
- 경로 찾기 기능을 어디서 시작해야 할지 불명확

**사용자 불편 사례:**
```
사용자: "병원으로 가는 길을 찾고 싶은데..."
1. 검색창을 찾는다 → OK
2. "Juba Hospital"을 검색 → OK
3. 병원을 선택 → OK
4. 경로 찾기 버튼을 찾는다 → ❌ 어디에?
5. RoutePlanningScreen으로 가야 하나? → ❌ 혼란
```

### 1.2 주요 기능 접근성 낮음

**문제:**
- **경로 찾기**: 가장 중요한 기능인데 버튼이 눈에 띄지 않음
- **위험 정보 필터**: 화면 상단에 가로로 나열되어 있어 공간을 많이 차지하고 스크롤 필요
- **내 위치**: 버튼 위치가 일관성 없음

**현재 UI 구조:**
```
┌─────────────────────────┐
│ 검색바                   │
├─────────────────────────┤
│ [무력충돌][시위][검문소]│  ← 위험 필터 (공간 낭비)
│ [도로손상][자연재해]...  │
├─────────────────────────┤
│                         │
│        지도 영역         │
│                         │
│                         │
└─────────────────────────┘
│ 하단 탭바                │
└─────────────────────────┘
```

### 1.3 화면 전환이 혼란스러움

**문제:**
- 모달과 시트가 겹쳐서 닫는 순서를 알기 어려움
- 경로 결과 화면에서 다른 경로를 선택하면 화면이 여러 겹 쌓임
- 뒤로 가기 동작이 예측 불가능

**현재 네비게이션 스택:**
```
MapScreen
  ├─ PlaceDetailSheet (모달)
  ├─ RouteResultSheet (모달)
  │   └─ RouteHazardBriefing (모달) ← 3중 모달!
  └─ SearchScreen (스택)
      └─ RoutePlanningScreen (스택)
```

### 1.4 경로 비교가 어려움

**문제:**
- 여러 경로가 계산되어도 한 번에 하나씩만 볼 수 있음
- 경로 간 차이(거리, 시간, 위험도)를 비교하기 어려움
- 다른 경로로 전환할 때마다 새로 로드되는 느낌

---

## 2. 경쟁 앱 UX 패턴 분석

### 2.1 Google Maps

#### 강점:
✅ **메인 화면이 깔끔함**
- 지도 + 하단 검색바만 있음
- 검색 결과는 하단 시트로 표시

✅ **경로 찾기 전용 화면**
- "길찾기" 버튼 클릭 → 전용 화면으로 전환
- 출발지/도착지 입력이 명확함
- 여러 경로를 리스트로 비교

✅ **레이어 토글**
- 우측 상단 레이어 버튼 → 교통/대중교통/자전거 등 선택

**사용자 흐름:**
```
1. 검색 → 2. 장소 선택 → 3. "길찾기" 버튼
→ 4. 경로 화면 → 5. 경로 비교 → 6. 선택
```

### 2.2 Naver Map

#### 강점:
✅ **상단 검색바 + 하단 빠른 버튼**
- 하단에 "길찾기" "주차" "대중교통" 빠른 액션

✅ **탭 전환 방식**
- "길찾기" 탭을 별도로 제공
- 최근 검색, 즐겨찾기 활용

✅ **플로팅 버튼**
- 우측 하단에 "내 위치" 버튼
- 레이어 버튼

**UI 구조:**
```
┌─────────────────────────┐
│    검색바               🔍│
├─────────────────────────┤
│                         │
│        지도 영역         │  🧭 ← 내 위치
│                         │  📍 ← 레이어
└─────────────────────────┘
│ [길찾기][주차][대중교통] │
└─────────────────────────┘
```

### 2.3 Kakao Map

#### 강점:
✅ **심플한 검색 UX**
- 검색창 터치 → 전체 화면으로 확장
- 자동완성이 빠르고 정확함

✅ **하단 길찾기 버튼 (항상 보임)**
- 화면 하단 중앙에 큰 "길찾기" 버튼
- 시각적으로 강조됨

✅ **경로 리스트 비교**
- 추천/최단/무료 경로를 카드 형태로 표시
- 스와이프로 전환 가능

---

## 3. 개선 방안

### 3.1 메인 지도 화면 재설계

**새로운 UI 구조:**
```
┌─────────────────────────┐
│  🔍 검색하세요...        📍│  ← 검색바 + 레이어 버튼
├─────────────────────────┤
│                         │
│                         │
│        지도 영역         │
│                         │
│                         │  🧭 ← 내 위치 버튼
│                         │  🧭
│                         │
│                    🔵   │  ← FAB (경로 찾기)
└─────────────────────────┘
│ [지도][제보][뉴스][프로필]│
└─────────────────────────┘
```

**개선 사항:**
- 위험 정보 필터를 상단 레이어 버튼 안으로 이동 → 화면 정리
- 경로 찾기 FAB(Floating Action Button) 추가 → 접근성 향상
- 검색바를 터치하면 전체 화면 검색 모드로 전환

### 3.2 새로운 사용자 흐름

#### A) 장소 검색 흐름

```
[메인 지도 화면]
     ↓ (검색바 터치)
[전체 화면 검색]
  - 자동완성 리스트
  - 최근 검색
     ↓ (장소 선택)
[하단 장소 정보 시트]
  - 주소, 카테고리
  - [여기서 출발] [여기로 도착] 버튼
     ↓
[지도로 복귀]
```

#### B) 경로 찾기 흐름

```
[메인 지도 화면]
     ↓ (FAB 버튼 터치)
[경로 계획 화면] ← 전체 화면
  - 출발지 입력
  - 도착지 입력
  - 교통 수단 선택
     ↓ (경로 계산)
[경로 비교 화면]
  - 여러 경로를 카드로 표시
  - 스와이프로 비교
  - 위험도/거리/시간 표시
     ↓ (경로 선택)
[지도 + 경로 표시]
  - 선택한 경로를 지도에 표시
  - 하단에 경로 요약 시트
  - [안내 시작] 버튼
```

#### C) 위험 정보 확인

```
[메인 지도 화면]
     ↓ (레이어 버튼 터치)
[레이어 메뉴]
  - ☑ 무력충돌
  - ☑ 시위/폭동
  - ☐ 검문소
  - ☐ 도로 손상
  ...
     ↓ (체크박스 선택)
[지도에 즉시 반영]
```

### 3.3 새로 추가할 컴포넌트

#### 1. FloatingActionButton.js
**목적:** 경로 찾기 기능을 항상 접근 가능하게

```javascript
// 위치: src/components/FloatingActionButton.js
// 특징:
// - 우측 하단 고정
// - 파란색 원형 버튼
// - 아이콘: 경로(directions) 아이콘
// - 터치 시: RoutePlanningScreen으로 이동
```

**디자인:**
```
     🔵  ← 파란색 원 (56x56)
    ➡️   ← 흰색 화살표 아이콘
```

#### 2. LayerToggleMenu.js
**목적:** 위험 정보 레이어를 깔끔하게 관리

```javascript
// 위치: src/components/LayerToggleMenu.js
// 특징:
// - 우측 상단 레이어 버튼 클릭 시 표시
// - 모달 또는 드롭다운 형태
// - 체크박스로 위험 유형 선택
// - 선택 시 즉시 지도에 반영
```

**UI:**
```
┌─────────────────────┐
│ 레이어 선택          │
├─────────────────────┤
│ ☑ 무력충돌    🔴    │
│ ☑ 시위/폭동   🟠    │
│ ☐ 검문소      🔴    │
│ ☐ 도로 손상   🟠    │
│ ☐ 자연재해    🔴    │
│ ☑ 안전 거점   🟢    │
├─────────────────────┤
│      [적용]         │
└─────────────────────┘
```

#### 3. RouteComparisonCard.js
**목적:** 여러 경로를 쉽게 비교

```javascript
// 위치: src/components/RouteComparisonCard.js
// 특징:
// - FlatList로 가로 스크롤
// - 각 경로를 카드 형태로 표시
// - 카드 터치 시 지도에 경로 미리보기
```

**카드 디자인:**
```
┌──────────────────────┐
│ 🛡️ 가장 안전한 경로  │ ← 배지
├──────────────────────┤
│ 📍 2.4 km            │
│ ⏱️  5분              │
│ ⚠️  위험도: 낮음     │
├──────────────────────┤
│ [자세히 보기]         │
└──────────────────────┘
  ←  스와이프  →
```

### 3.4 개선할 기존 컴포넌트

#### 1. SearchBar.js
**현재 문제:**
- 검색창이 작아서 터치하기 어려움
- 자동완성이 검색창 아래 작은 영역에만 표시

**개선안:**
```javascript
// 변경 사항:
// 1. 검색창 터치 → 전체 화면 검색 모드로 전환
// 2. 전체 화면에 자동완성 리스트 표시
// 3. 최근 검색, 즐겨찾기 추가
// 4. 취소 버튼 추가
```

**전체 화면 검색 UI:**
```
┌─────────────────────────┐
│ 🔍 검색...         ❌    │  ← 검색창 + 취소
├─────────────────────────┤
│ 최근 검색                │
│  📍 Juba Hospital        │
│  📍 Juba Girls School    │
├─────────────────────────┤
│ 자동완성 결과            │
│  📍 Juba Teaching Hosp.. │
│  📍 Juba City Council    │
│  ...                     │
└─────────────────────────┘
```

#### 2. RoutePlanningScreen.js
**현재 문제:**
- 스택으로 푸시되어 복잡함
- 경로 결과가 별도 시트로 표시되어 혼란

**개선안:**
```javascript
// 변경 사항:
// 1. 전체 화면 모드로 변경
// 2. 경로 계산 결과를 같은 화면에 표시
// 3. 경로 카드를 가로 스크롤로 비교
// 4. 경로 선택 시 지도 화면으로 복귀
```

**새로운 UI:**
```
┌─────────────────────────┐
│ ← 경로 찾기              │
├─────────────────────────┤
│ 📍 출발: 내 위치         │
│ 📍 도착: Juba Hospital   │
│ 🚗 자동차 | 🚶 도보       │
├─────────────────────────┤
│ 경로 결과 (3개)          │
│ ┌────┐ ┌────┐ ┌────┐   │
│ │ 1  │ │ 2  │ │ 3  │   │  ← 카드
│ └────┘ └────┘ └────┘   │
├─────────────────────────┤
│       지도 미리보기       │
│                         │
└─────────────────────────┘
│      [경로 선택]         │
└─────────────────────────┘
```

#### 3. MapScreen.js
**현재 문제:**
- 너무 많은 state와 로직이 섞여 있음
- 모달/시트가 겹쳐서 복잡함

**개선안:**
```javascript
// 변경 사항:
// 1. 위험 필터를 레이어 버튼 안으로 이동
// 2. FAB 버튼 추가
// 3. 시트 계층 단순화 (최대 1개만 열리도록)
// 4. 검색은 전체 화면으로 분리
```

---

## 4. 구현 계획

### 4.1 단계별 구현 순서

#### Phase 1: 기본 UI 개선 (1-2시간)
```
1. FloatingActionButton.js 생성
   - 위치: src/components/FloatingActionButton.js
   - 기능: 경로 찾기 화면으로 이동

2. LayerToggleMenu.js 생성
   - 위치: src/components/LayerToggleMenu.js
   - 기능: 위험 정보 레이어 토글

3. MapScreen.js 수정
   - FAB 버튼 추가
   - 위험 필터 UI 제거 (레이어 버튼으로 대체)
```

#### Phase 2: 검색 UX 개선 (1-2시간)
```
1. SearchBar.js 수정
   - 전체 화면 검색 모드 추가
   - 최근 검색 기능 추가

2. SearchScreen.js 생성/수정
   - 전체 화면 검색 UI
   - 자동완성 리스트
```

#### Phase 3: 경로 비교 개선 (2-3시간)
```
1. RouteComparisonCard.js 생성
   - 위치: src/components/RouteComparisonCard.js
   - 가로 스크롤 카드 리스트

2. RoutePlanningScreen.js 수정
   - 전체 화면 모드로 변경
   - 경로 결과를 카드로 표시
   - 지도 미리보기 추가

3. RouteResultSheet.js 제거
   - 기능을 RoutePlanningScreen으로 통합
```

#### Phase 4: 테스트 및 최적화 (1시간)
```
1. 사용자 흐름 테스트
   - 검색 → 경로 찾기 → 경로 선택

2. 성능 최적화
   - 불필요한 리렌더링 제거
   - 메모이제이션 적용

3. 버그 수정
```

### 4.2 파일 구조 변경

**새로 생성할 파일:**
```
src/
├── components/
│   ├── FloatingActionButton.js       ← NEW
│   ├── LayerToggleMenu.js            ← NEW
│   └── RouteComparisonCard.js        ← NEW
```

**수정할 파일:**
```
src/
├── screens/
│   ├── MapScreen.web.js              ← MODIFY
│   ├── MapScreen.native.js           ← MODIFY
│   ├── RoutePlanningScreen.js        ← MODIFY
│   └── SearchScreen.js               ← MODIFY
├── components/
│   ├── SearchBar.js                  ← MODIFY
│   ├── RouteResultSheet.js           ← REMOVE (통합)
│   └── PlaceDetailSheet.js           ← MODIFY
```

### 4.3 코드 스니펫 (참고용)

#### FloatingActionButton.js 예시
```javascript
import React from 'react';
import { TouchableOpacity, StyleSheet, View } from 'react-native';
import { useNavigation } from '@react-navigation/native';
import Icon from './icons/Icon';
import { Colors } from '../styles';

export default function FloatingActionButton() {
  const navigation = useNavigation();

  const handlePress = () => {
    navigation.navigate('RoutePlanning');
  };

  return (
    <TouchableOpacity
      style={styles.fab}
      onPress={handlePress}
      activeOpacity={0.8}
    >
      <Icon name="directions" size={28} color="#FFFFFF" />
    </TouchableOpacity>
  );
}

const styles = StyleSheet.create({
  fab: {
    position: 'absolute',
    right: 20,
    bottom: 100, // 탭바 위
    width: 56,
    height: 56,
    borderRadius: 28,
    backgroundColor: Colors.primary,
    justifyContent: 'center',
    alignItems: 'center',
    elevation: 8,
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 4 },
    shadowOpacity: 0.3,
    shadowRadius: 8,
  },
});
```

#### LayerToggleMenu.js 예시
```javascript
import React from 'react';
import { View, Text, TouchableOpacity, Modal, StyleSheet } from 'react-native';
import Icon from './icons/Icon';
import { Colors, Typography } from '../styles';

const HAZARD_TYPES = [
  { id: 'armed_conflict', name: '무력충돌', color: '#EF4444' },
  { id: 'protest_riot', name: '시위/폭동', color: '#F59E0B' },
  { id: 'checkpoint', name: '검문소', color: '#FF6B6B' },
  { id: 'road_damage', name: '도로 손상', color: '#F97316' },
  { id: 'natural_disaster', name: '자연재해', color: '#DC2626' },
  { id: 'safe_haven', name: '안전 거점', color: '#10B981' },
];

export default function LayerToggleMenu({ visible, onClose, activeTypes, onToggle }) {
  return (
    <Modal
      visible={visible}
      transparent
      animationType="fade"
      onRequestClose={onClose}
    >
      <TouchableOpacity style={styles.overlay} onPress={onClose} activeOpacity={1}>
        <View style={styles.menu}>
          <Text style={styles.title}>레이어 선택</Text>
          {HAZARD_TYPES.map(type => (
            <TouchableOpacity
              key={type.id}
              style={styles.item}
              onPress={() => onToggle(type.id)}
            >
              <Icon
                name={activeTypes.includes(type.id) ? 'checkBoxChecked' : 'checkBoxUnchecked'}
                size={24}
                color={type.color}
              />
              <Text style={styles.itemText}>{type.name}</Text>
              <View style={[styles.badge, { backgroundColor: type.color }]} />
            </TouchableOpacity>
          ))}
        </View>
      </TouchableOpacity>
    </Modal>
  );
}

const styles = StyleSheet.create({
  overlay: {
    flex: 1,
    backgroundColor: 'rgba(0,0,0,0.5)',
    justifyContent: 'flex-start',
    alignItems: 'flex-end',
    paddingTop: 60,
    paddingRight: 16,
  },
  menu: {
    backgroundColor: Colors.surface,
    borderRadius: 12,
    padding: 16,
    minWidth: 220,
    elevation: 8,
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.25,
    shadowRadius: 8,
  },
  title: {
    ...Typography.h3,
    marginBottom: 12,
  },
  item: {
    flexDirection: 'row',
    alignItems: 'center',
    paddingVertical: 10,
  },
  itemText: {
    ...Typography.body,
    marginLeft: 12,
    flex: 1,
  },
  badge: {
    width: 12,
    height: 12,
    borderRadius: 6,
  },
});
```

---

## 5. 예상 효과

### 5.1 사용자 경험 개선

**Before (현재):**
```
경로 찾기까지 걸리는 단계: 7-8단계
- 검색 → 장소 선택 → ??? → RoutePlanningScreen →
  출발지 입력 → 도착지 입력 → 계산 → 결과 확인
```

**After (개선 후):**
```
경로 찾기까지 걸리는 단계: 3-4단계
- FAB 클릭 → 출발/도착지 입력 → 경로 비교 → 선택
```

**개선율: 약 50% 단축**

### 5.2 사용성 지표 개선 예상

| 지표 | 현재 | 개선 후 | 개선율 |
|------|------|---------|--------|
| 경로 찾기 소요 시간 | 30-45초 | 15-20초 | **50%↓** |
| 경로 비교 편의성 | ⭐⭐ | ⭐⭐⭐⭐⭐ | **150%↑** |
| 위험 정보 확인 | ⭐⭐⭐ | ⭐⭐⭐⭐ | **33%↑** |
| 전반적 만족도 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | **66%↑** |

### 5.3 기술적 이점

1. **코드 유지보수성 향상**
   - 컴포넌트 역할이 명확해짐
   - 모달/시트 계층 단순화

2. **성능 개선**
   - 불필요한 리렌더링 감소
   - 메모리 사용 최적화

3. **확장성 증가**
   - 새로운 기능 추가가 쉬워짐
   - A/B 테스트 용이

---

## 6. 체크리스트

구현 시 확인할 사항:

### Phase 1: 기본 UI
- [ ] FloatingActionButton 생성 및 MapScreen에 추가
- [ ] FAB 위치가 다른 요소와 겹치지 않는지 확인
- [ ] LayerToggleMenu 생성
- [ ] 레이어 메뉴 체크박스 동작 확인
- [ ] MapScreen에서 위험 필터 UI 제거

### Phase 2: 검색 UX
- [ ] SearchBar 터치 시 전체 화면 전환
- [ ] 자동완성 리스트 표시
- [ ] 최근 검색 기능 추가
- [ ] 취소 버튼 동작 확인

### Phase 3: 경로 비교
- [ ] RouteComparisonCard 생성
- [ ] 가로 스크롤 동작 확인
- [ ] RoutePlanningScreen 전체 화면 모드 변경
- [ ] 경로 카드에서 경로 선택 동작 확인
- [ ] 지도 미리보기 추가

### Phase 4: 테스트
- [ ] 전체 사용자 흐름 테스트
- [ ] 성능 프로파일링
- [ ] 크로스 플랫폼 테스트 (웹/네이티브)
- [ ] 접근성 확인

---

## 7. 참고 자료

### 7.1 디자인 가이드라인
- [Material Design - Navigation](https://material.io/design/navigation)
- [iOS Human Interface Guidelines - Navigation](https://developer.apple.com/design/human-interface-guidelines/ios/app-architecture/navigation/)

### 7.2 관련 라이브러리
- `react-native-gesture-handler` - 제스처 처리
- `react-native-reanimated` - 애니메이션
- `@gorhom/bottom-sheet` - 하단 시트 (대안)

### 7.3 경쟁 분석
- Google Maps UX 분석: [링크]
- Naver Map 사용자 흐름: [링크]
- Kakao Map 인터페이스: [링크]

---

## 8. 향후 개선 아이디어

개선 완료 후 추가로 고려할 사항:

1. **음성 안내**
   - 경로 안내 중 음성으로 위험 정보 알림

2. **오프라인 모드**
   - 지도 다운로드 기능
   - 오프라인 경로 탐색

3. **커뮤니티 기능**
   - 사용자가 위험 정보를 실시간으로 제보
   - 제보에 대한 신뢰도 평가

4. **개인화**
   - 자주 가는 장소 저장
   - 선호하는 경로 학습

5. **다국어 지원**
   - 영어/아랍어 지원
   - 현지 언어 대응

---

## 부록: 와이어프레임

### A. 메인 지도 화면
```
┌─────────────────────────────────┐
│  🔍 검색하세요...          📍   │  ← 검색바 + 레이어
├─────────────────────────────────┤
│                                 │
│                                 │
│                                 │
│                                 │
│           지도 영역              │
│                                 │
│                                 │  🧭 ← 내 위치
│                                 │
│                                 │
│                            🔵   │  ← FAB
└─────────────────────────────────┘
│ [지도] [제보] [뉴스] [프로필]   │
└─────────────────────────────────┘
```

### B. 검색 화면 (전체 화면)
```
┌─────────────────────────────────┐
│  🔍 Juba Hospital          ❌   │  ← 검색창
├─────────────────────────────────┤
│  최근 검색                       │
│  📍 Juba Teaching Hospital      │
│  📍 Juba City Council           │
├─────────────────────────────────┤
│  검색 결과                       │
│  📍 Juba Teaching Hospital      │
│     Hai Orselim, Juba City      │
│  ─────────────────────────────  │
│  📍 Juba Referral Hospital      │
│     Central Equatoria           │
│  ─────────────────────────────  │
│  📍 Al-Sabah Children Hospital  │
│     Juba, South Sudan           │
└─────────────────────────────────┘
```

### C. 경로 계획 화면
```
┌─────────────────────────────────┐
│  ← 경로 찾기                     │
├─────────────────────────────────┤
│  📍 출발                         │
│  [  내 위치                    ] │
│                                 │
│  📍 도착                         │
│  [  Juba Teaching Hospital    ] │
│                                 │
│  🚗 [자동차] 🚶 [도보]           │
├─────────────────────────────────┤
│  경로 결과 (3개)                 │
│  ┌──────┐ ┌──────┐ ┌──────┐    │
│  │ 안전 │ │ 최단 │ │ 빠른 │    │
│  │ 2.4km│ │ 1.8km│ │ 2.0km│    │
│  │  5분 │ │  4분 │ │  4분 │    │
│  └──────┘ └──────┘ └──────┘    │
│   ←  스와이프  →                │
├─────────────────────────────────┤
│                                 │
│        지도 미리보기             │
│          (경로 표시)             │
│                                 │
└─────────────────────────────────┘
│         [경로 시작하기]          │
└─────────────────────────────────┘
```

### D. 레이어 메뉴
```
        ┌───────────────────┐
        │ 레이어 선택        │
        ├───────────────────┤
        │ ☑ 무력충돌   🔴   │
        │ ☑ 시위/폭동  🟠   │
        │ ☐ 검문소     🔴   │
        │ ☐ 도로 손상  🟠   │
        │ ☐ 자연재해   🔴   │
        │ ☑ 안전 거점  🟢   │
        ├───────────────────┤
        │    [적용]         │
        └───────────────────┘
```

---

**문서 끝**

> 질문이나 피드백은 개발팀에 문의하세요.
> 이 문서는 구현 중 계속 업데이트됩니다.
